package app.developer.uiview.progress_bar;

/**
 * The enum Type progress.
 */
public enum TypeProgress {
    defaultProgress,
    innerProgress,
    externalProgress,
    singleExternalProgressDefault,
    singleInnerProgressDefault,
    singleExternalProgress,
    singleExternalProgressWithAnomaly,
    trackingIndicators,
    upslope
}
